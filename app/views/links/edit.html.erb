<%= form_for(@link, url: user_link_path(id: @link.id, user_id:current_user.id)) do |f| %>
    <table>
      <thead>
      <tr>
        <th><%= f.label :url %></th>
        <th><%= f.label :title %></th>
      </tr>
      </thead>
      <tbody>
      <tr>
        <td><%= f.text_field :url %></td>
        <td><%= f.text_field :title %></td>
      </tr>
      <tr>
        <th></th>
        <th><%= f.submit %></th>
      </tr>
      </tbody>
    </table>
<% end %>